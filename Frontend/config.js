// Optional — index.html now sets RENTAL_API_BASE inline (avoids stale localhost on Railway).
// Kept for reference / older tooling.
window.RENTAL_API_BASE = window.RENTAL_API_BASE || 'http://127.0.0.1:8000';
